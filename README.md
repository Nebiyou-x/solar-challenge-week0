## 🌞 Streamlit Dashboard (Bonus Task)

An interactive web app built with **Streamlit** to compare solar potential across Benin, Sierra Leone, and Togo.

### Run Locally
```bash
pip install -r requirements.txt  # ensure streamlit & plotly included
streamlit run app/main.py
